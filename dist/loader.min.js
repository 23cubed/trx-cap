(function() { // TRX Cap Dynamic Loader - Auto-loads latest version const REPO = '23cubed/trx-cap'; const FALLBACK_VERSION = '1.0.3'; function loadScript(url) { const script = document.createElement('script'); script.src = url; script.async = true; script.onload = () => console.log('🚀 TRX Cap loaded from:', url); script.onerror = () => { console.warn('Failed to load from:', url, 'trying fallback...'); if (url.includes('@v')) return; // Already tried versioned loadScript(`https://cdn.jsdelivr.net/gh/${REPO}@v${FALLBACK_VERSION}/dist/main.js`); }; document.head.appendChild(script); } // Try to load latest, fallback to versioned loadScript(`https://cdn.jsdelivr.net/gh/${REPO}/dist/main.js`); })();